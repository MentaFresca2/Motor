#version 460 core
out vec4 FragColor;

void main()
{
    FragColor = vec4(1.0,1.0,1.0,1.0); // set all 4 vector values to 1.0 Esto es el colo que tendra el objeto que de la luz (O que de la sensacion que da luz)
}
